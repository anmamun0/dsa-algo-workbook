#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    sort(s.begin(), s.end());

    cout << s << endl;

    // cout << *s.begin() << " "<<*(s.end()-1);

    return 0;
}