#include<stdio.h>
int main(){

    char a[6];

    // scanf("%s", &a); // &a and just a , all right
    scanf("%s", a);

    printf("%s\n", a);


    
    // printf("%d", sizeof(a) / sizeof(char));

    return 0;
}