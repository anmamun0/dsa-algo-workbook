if(fast[length_small != '\0']){
                //     printf("%c", fast[j]);
                // }
                // else if (second[length_small] != '\0')
                // {
                // printf("%c", second[j]);

                // }